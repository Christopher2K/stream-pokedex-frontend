import { css } from "@emotion/react";

export const root = css`
  * {
    font-family: "Raleway", sans-serif;
  }

  #root {
    width: 100%;
    margin: 0 auto;
  }

  body,
  #root {
    min-height: 100%;
  }
`;
